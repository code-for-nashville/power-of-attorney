language: node_js
node_js:
  - "8"
cache:
  yarn: true
  directories:
    - "node_modules"
script:
  - yarn build
  - yarn lint
  - yarn test
deploy:
  provider: pages
  skip-cleanup: true
  github-token: $GITHUB_TOKEN
  keep-history: true
  on:
    branch: master
